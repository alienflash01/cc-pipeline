repo: /tmp/cc-pipeline-blackbox/fake-repo
base_branch: main
concurrency: 1
max_retries: 1
snippets:
    hi: "echo hi"
pipeline:
  - id: main
    executor: shell
    prompt_file: /tmp/pw-test/prompts/valid.md
    loop: per_file
    retry: 1
    postcondition:
      shell: "echo ok"
      expect: "false"
    output: .pipeline/pw17.json
modules:
  - name: m1
    source_dir: src
    source_files: ["*.c"]
    file_order: sequential
    continue_on_error: true