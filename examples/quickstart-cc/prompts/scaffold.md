# Scaffold step

You are setting up the test scaffold for the `{module}` module.

- Read every `.py` file under `{source_dir}`.
- Create a `tests/` directory if it does not exist.
- Add an empty `tests/test_{module}.py` ready to receive pytest cases.
- Do NOT write the test bodies — that happens in the generate step.
